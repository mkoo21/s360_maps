'use-strict';

import React from 'react';
import {
    View,
    Text,
    ListView,
    StyleSheet
} from 'react-native';

//Implement this if we want to have a static section displayed at the bottom of the sidemenu (what's a good place to dump a settings menu?)

var MenuStaticSection = React.createClass({
    render() {

    }
});

module.exports=MenuStaticSection;

var styles = StyleSheet.create({

})